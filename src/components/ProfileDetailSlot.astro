---
const { title } = Astro.props;
---

<div class="flex flex-col gap-1 text-center sm:text-left items-center sm:items-start">
  <h1 class="text-base sm:text-lg font-semibold uppercase text-muted">{title}</h1>
  <slot />
</div>
